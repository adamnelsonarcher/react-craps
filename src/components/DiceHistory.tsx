import React from 'react';

interface DiceRoll {
  die1: number;
  die2: number;
  total: number;
}

const DiceHistory: React.FC<{ rolls: DiceRoll[] }> = ({ rolls }) => {
  return ( null
  );
};

export default DiceHistory; 